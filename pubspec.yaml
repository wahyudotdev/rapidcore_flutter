name: rapidcore_flutter
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/wahyudotdev/rapidcore_flutter

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  data_connection_checker:
    git: git@github.com:uSlashVlad/data_connection_checker.git

flutter:
