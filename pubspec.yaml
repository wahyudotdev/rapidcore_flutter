name: rapidcore_flutter
description: Core library for rapid development.
version: 0.0.2
homepage: https://github.com/wahyudotdev/rapidcore_flutter

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  infinite_scroll_pagination: ^3.2.0

flutter:
